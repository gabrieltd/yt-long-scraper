# Local PostgreSQL via Docker. Keep the real file untracked.
# URL-encode reserved password characters before setting this value.
DATABASE_URL=postgresql://yt_user:replace-with-url-encoded-password@localhost:5432/yt_discovery
